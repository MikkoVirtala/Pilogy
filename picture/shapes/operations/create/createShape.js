module.exports.createShape = (style, typeOfShape) => {
	return
}

const createLine = () => {
	return new Line(
		length,
		stroke,
		strokeOpacity,
		strokeWidth,
		rotationInDegrees,
		pointToAttach,
		attachmentPointIndex,
		visible,
		parentId
	)
}
